# Mike Express + TypeScript

Minimal Express + TypeScript starter.

Commands:

```powershell
cd /d D:\mike
npm install
npm run dev    # development with auto-reload
npm run build  # compile to dist/
npm start      # run compiled app
```

Server entry: src/index.ts
